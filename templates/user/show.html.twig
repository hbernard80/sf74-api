{% extends 'base.html.twig' %}

{% set page_title = ('user.profile_me'|trans({}, 'main'))|capitalize %}

{% block title %}{{ page_title }} - {{ parent() }}{% endblock %}

{% block body %}
    <h1 class="border-bottom mb-4">{{ page_title }}</h1>
    {{ dump(user) }}
{% endblock %}